{
  "status": "STUDIED_NOT_ACCEPTED",
  "scope": "Double Big Jump composition",
  "principle": "Two canonical Big Jump waves composed concurrently; not a second flight architecture.",
  "canonical_slots": ["F1", "F2", "F3", "F4", "F5"],
  "required_invariants": [
    "explicit mission assignment per reusable slot",
    "shared verified current repository HEAD",
    "independent wave evidence",
    "fail-closed on missing or stale HEAD",
    "independent wave PASS required for combined PASS"
  ],
  "note": "Architecture contract added for subsequent integration; existing execution paths still require verification before promotion."
}
